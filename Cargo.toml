[package]
name = "tar-vault-sync"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full", "test-util"] }

[dev-dependencies]
tempfile = "3"
